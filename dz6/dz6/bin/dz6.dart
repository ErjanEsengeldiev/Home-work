void main() {
  String lang = 'en';
  List<String> arr = [];
  List<String> enDays = ['Monday', '...'];
  List<String> ruDays = ['Понидельник', '...'];
  for (int i = 0; i < enDays.length; i++);
  if (lang.contains('en')) {
    print(enDays);
  }
  for (int i = 0; i < ruDays.length; i++);
  if (lang.contains('ru')) {
    print(ruDays);
  }
}
